/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['media.istockphoto.com', 'cdn.pixabay.com', 'api.lorem.space', 'burst.shopifycdn.com', 'mir-s3-cdn-cf.behance.net']
  },
  reactStrictMode: true,
}

module.exports = nextConfig
